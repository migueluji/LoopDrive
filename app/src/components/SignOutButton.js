// components/SignOutButton.js

import React from 'react';

const SignOutButton = ({ onClick }) => {
  return <button className="signout" onClick={onClick}>Sign Out</button>;
};

export default SignOutButton;
