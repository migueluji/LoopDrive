// components/SignInButton.js

import React from 'react';

const SignInButton = ({ onClick }) => {
  return <button className="signin" onClick={onClick}>Sign In</button>;
};

export default SignInButton;
